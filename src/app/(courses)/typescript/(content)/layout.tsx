import { PropsWithChildren } from 'react';

const TypeScriptContent = ({ children }: PropsWithChildren) => {
  return <div className="mdx-wrapper">{children}</div>;
};

export default TypeScriptContent;
