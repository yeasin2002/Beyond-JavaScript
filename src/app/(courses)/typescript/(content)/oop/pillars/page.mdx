import { CodeBlock } from '../../../../../../components/ui/custom/code-blocks';

### আমরা আগেও জানলাম, TypeScript OOP এর মুল বিষয় বা পিলার সমূহ হলোঃ

        1. classes
        2. inheritance
        3. polymorphism
        4. encapsulation
        5. abstraction
        6. interface

এই অধ্যায়ে আমরা প্রতিটূল পিলার বিস্তারিত আলোচনা করব ।


## Class 
 `Class` হল কোনো অবজেক্ট বানানোর জন্য ফ্যাক্টরি বা  নীল-নকশা , আগের অধ্যায়ে যেহেতু আলোচনা করা হয়েছে তাই এই অধ্যায়ে এটি নিয়ে বিস্তারিত লিখে প্রয়োজন নেই ।

##  inheritance :
 অন্য কোণ ক্লাস এর মেথদ বা প্রোপার্টী কোণ ক্লাসে নিয়ে আসে তা একসেস বা ব্যবহার করাকে inheritance বলা হয় । উদাহরণ সহ এখন দেখিঃ

<CodeBlock language="TypeScript" 
    code={`
        class Animal {
        name: string;

        constructor(name: string) {
        this.name = name;
        }

        makeSound(): void {
        console.log("Animal makes a sound");
        }
        }

       // class that inheriting Animal
       class Dog extends Animal {
       makeSound(): void {
       console.log("Woof! Woof!");
         }
        }

`} />

   এখানে Dog ক্লাস Animal ক্লাসকে `extends` keyword এর মাধ্যমে এর property এবং methods extends করছে । এক্ষাত্রে Animal হল Parent Class ও Dog হল Child Class ।  
   এটাকে এটাকে কিভাবে ব্যবহার করব তা দেখি । 

<CodeBlock language="TypeScript" 
  code={`
 // Create an instance of Dog
 const myDog = new Dog("Buddy");
 console.log(myDog.name); // Output: Buddy
 myDog.makeSound(); // Output: Woof! Woof!
`}
   /> 

যদিও Dog Class এর নিজের কোন `makeSound` নামের কোন method নাই কিন্তু তবুও আমরা ব্যবহার করতে পারছি কারন এটা তার প্যারেন্ট অর্থাৎ Animal Class এর থেকে নিয়েছে । 




## polymorphism 